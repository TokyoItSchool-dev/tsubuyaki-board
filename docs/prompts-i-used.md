# 使用したプロンプト記録

このファイルは、社内つぶやきボードの実装中に Codex へ依頼した内容を記録するためのメモです。

## セットアップ・起動確認

- このリポジトリで何を作ろうとしているか確認した。
- コンテナ内で開発しているか確認した。
- フォルダ内でどのようなセットアップや処理が動作しているか、初学者向けに説明してもらった。
- 最低限アプリが動作するか確認した。
- `./mvnw -Ph2 spring-boot:run` で Spring Boot を起動し、`localhost` の確認リンクを出してもらった。
- 画面が確認できない場合の原因を確認した。
- コンテナがどのように起動しているか確認した。

## 投稿一覧機能

- 投稿一覧表示機能を作成してもらった。
- 新着順 `created_at DESC` で最大 50 件を返すように依頼した。
- `posts/list.html` を使うように依頼した。
- 投稿が 0 件の場合に「まだ投稿はありません」を表示するように依頼した。
- `model.posts` に `List` を積むように依頼した。
- 更新ボタンを追加し、押すとページをリロードするように依頼した。
- 投稿者、内容、投稿日の順に表示するように依頼した。
- 内容が長い場合に適度に折り返すように依頼した。
- 投稿一覧機能が実装されているか、どこを確認すればよいか説明してもらった。

## 投稿数・省略表示・XSS 対策

- 「新着で 50 件を返す」処理の確認のため、投稿数を 102 件まで増やすよう依頼した。
- 投稿が 100 文字を超える場合は `....` で省略するよう依頼した。
- `<script>alert('危険')</script>` のような悪意ある JavaScript が画面で実行されないように依頼した。
- 悪意ある JavaScript タグが記載されている場合、投稿ボタンを押せないように依頼した。
- 投稿を 102 個まで増やしている追加処理の場所と仕組みを説明してもらった。

## ページング・レイアウト・ツールチップ

- ページ遷移リンクを作成するよう依頼した。
- 1 ページ 50 件の投稿を表示するよう依頼した。
- 画面 CSS を修正し、清潔感あるレイアウトに変更するよう依頼した。
- 画面右下に TOP へ戻るボタンを実装するよう依頼した。
- 100 文字以上の投稿を 3 件追加するよう依頼した。
- 省略表示された投稿にホバーしたとき、ツールチップが動作するよう依頼した。
- ツールチップが動作していないように見える点を指摘し、修正してもらった。

## 投稿作成フォーム・投稿登録

- 投稿作成フォームと投稿登録処理を実装するよう依頼した。
- `posts/form.html` を表示するよう依頼した。
- フォームバインド用に `postForm` を model に積むよう依頼した。
- `author` は 1 から 30 文字、`body` は 1 から 280 文字、空白のみは NG にするよう依頼した。
- 成功時は 302 で `/posts` にリダイレクトするよう依頼した。
- 失敗時は 200 で `posts/form` を再表示し、エラーを表示するよう依頼した。
- `result.hasErrors()` がある場合は保存ロジックをスキップするよう依頼した。
- チェック通過後に `Post` Entity を作成し、setter で値を詰め替え、`postRepository.save(entity)` で保存するよう依頼した。
- 未入力、上限超え、空白のみ、入力値保持、エラーメッセージ表示、正常登録、DB 件数増加のテストを用意して実装するよう依頼した。
- 現状の diff 全容と、一文一文の機能説明を出してもらった。
- `id` が登録者名ではなく投稿の識別子か確認した。

## 投稿詳細・編集・削除・ごみ箱

- 投稿詳細処理を実装するよう依頼した。
- `posts/detail.html` を表示するよう依頼した。
- 存在しない `id` は 404 にするよう依頼した。
- 一覧の `id` をクリックしたら詳細へ遷移するよう依頼した。
- `id` を画面上に表示するよう依頼した。
- 詳細画面に投稿者名、投稿内容全文、投稿日時、編集ボタン、削除ボタンを表示するよう依頼した。
- 画面左側に、編集・削除された投稿 ID を表示するお知らせ欄を作成するよう依頼した。
- 新規投稿ボタンの横にごみ箱ボタンを作成するよう依頼した。
- 削除された投稿は DB から物理削除せず、ごみ箱に格納するよう依頼した。
- 更新を行うとお知らせ欄の中身が消える問題を指摘し、修正してもらった。
- 存在しない ID の 404 確認方法として `/posts/1045` や `/posts/999999` で確認できるか質問した。

## ヘルスチェック

- M5 のヘルスチェックとして `/actuator/health` が `UP` を返すことの意味を確認した。

## フォーム CSS 修正

- `/posts/new` のページでテキストエリアの大きさを変更した際、背景の CSS が入力欄の大きさに合わせて変化するよう修正を依頼した。
- `form-field` の `input` と `textarea` で CSS を共有している点を確認した。
- `post-form` も同様に横幅を広げ、`box-sizing: border-box;` を入れるよう依頼した。
- `resize` で変化した見た目が `form-field` の中身に収まるよう依頼した。

## いいね機能

- 投稿のいいね機能を実装するよう依頼した。
- `POST /posts/{id}/likes` でいいねをトグルするよう依頼した。
- 同一 `clientHash`、つまりハッシュ化された IP と UA の SHA-256 先頭 8 文字が再度押したら解除するよう依頼した。
- 詳細画面でいいね数と Like ボタンを表示するよう依頼した。
- `POST /posts/{id}/likes` の `{id}` が動的に動くか確認した。

## キーワード検索

- 投稿のキーワード検索機能を実装するよう依頼した。
- `GET /posts?q=xxx` で本文 LIKE 検索するよう依頼した。
- 一覧画面を再利用し、上部に検索ボックスを追加するよう依頼した。
- 検索ボックスは更新ボタンの左側に置くよう依頼した。
- 部分検索で全角半角を区別しないよう依頼した。
- 空文字、空白のみ、空文字と空白のみの場合は検索ボタンを非活性にするよう依頼した。
- 検索ボックス内に `<` や `>` のようなタグ疑い文字がある場合も非活性にするよう依頼した。

## アバター機能

- 新規登録機能を改修し、投稿時に自分のアバター色を選択できるよう依頼した。
- 投稿一覧で、投稿者名の右側に円形で選択カラーを表示するよう依頼した。
- 円形のアバター表示箇所に画像も表示できるよう依頼した。
- 新規投稿時に画像アップロード機能を追加するよう依頼した。
- 画像をアップロードした場合はカラー選択不要にするよう依頼した。
- 画像をアップロードしない場合はカラー選択必須にするよう依頼した。
- カラー選択と画像アップロードは同時にできないよう依頼した。
- 既存投稿を編集してアバター色を変更してもグレーのままだった問題を指摘し、編集時にもアバター情報を更新するよう修正してもらった。

## ドキュメント

- これまで送ったプロンプトが `docs/prompts-i-used.md` に記載されているか確認した。
- `docs/prompts-i-used.md` が存在しなかったため、作成を依頼した。
